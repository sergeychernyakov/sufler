# requirements-dev.txt — development & quality toolchain
#
# Install everything with:  bin/setup
# Pins for tools that drive the gate are kept in sync with .pre-commit-config.yaml.

# Git hooks
pre-commit==4.2.0

# Format
black==24.10.0
isort==5.13.2

# Lint
ruff==0.8.6
pylint==3.3.2

# Types
mypy==1.11.2

# Security
bandit[toml]>=1.7
pip-audit>=2.7

# Tests + coverage
pytest==8.4.2
pytest-cov>=5.0
coverage==7.8.2
