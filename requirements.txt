# requirements.txt — runtime dependencies (the application itself)
#
# Dev/quality tooling lives in requirements-dev.txt.
# Do NOT `pip freeze` into this file — keep it to direct, intentional deps.

# GUI / overlay
PyQt6>=6.6

# Audio capture
sounddevice>=0.4
numpy>=1.26

# Screenshots
mss>=9.0

# Global hotkeys
pynput>=1.7

# LLM (Claude)
anthropic>=0.40

# Config / models
python-dotenv>=1.0
pydantic>=2.7

# ── Speech-to-text (Apple Silicon, Phase 5) ──
# Default engine. whisper.cpp (external Metal binary/server) is an alternative — see README.
mlx-whisper>=0.4          # native Apple Silicon Whisper
